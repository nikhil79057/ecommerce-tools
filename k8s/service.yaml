apiVersion: v1
kind: Service
metadata:
  name: saas-tools-service
  namespace: saas-tools
spec:
  selector:
    app: saas-tools
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP